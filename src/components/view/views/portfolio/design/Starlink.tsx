import { Component } from 'preact';
import View from '../../../View';

export default class Starlink extends Component {
  render() {
    return (
      <View name='starlink'>
        <span>Starlink</span>
      </View>
    );
  }
}